import by.IT.Person;

import java.util.Arrays;
import java.util.List;

import java.util.stream.Collector;
///public class Runner_exm2<list1> {
////  List<Person> personList = Arrays.asList(
////     new Person("Alex", 23),
////    new Person("Bob", 20),
////    new Person("Kate", 25),
///    new Person("Alex", 22),
///    new Person("Bob", 27),
////    new Person("Alex", 29)

///  );
 ////  Map<String,List<Person>> map = new HashMap<>();
  /// for (Person person : personList){
  ///     List<Person> personList=
    ///           map.getOrDefault(person.getName(), new ArrayList<>());
     ///  personList.add(person);
     ////  map.put(person.getName(), valueList);

////}
   ////for (Map.Entry<String, List<Person>> entry : map.entrySet()){
     ////   System.out.println("name:" + entry.getKey());
      ///  System.out.println(Person :" + entry.gentValue() + "\n");
   /// }

   ////java 8


   /// public Map.Entry<String, List<Person>> getEntry() {

     ///   return entry;
  //  }
//}

///}